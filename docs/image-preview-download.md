# 🖼️ 图片预览和下载功能

## 功能特性

### 1. 悬停操作按钮

- 鼠标悬停显示下载和放大按钮
- 半透明白色背景，平滑淡入淡出

### 2. 放大预览

- 对话框查看高清大图（最大 1280px 宽）
- 显示标题和说明
- 对话框内可下载

### 3. 图片下载

- 直接保存图片到本地
- 文件名使用 alt 属性
- 支持所有格式（含 base64）

## 支持的布局

### 单张图片

- 悬停显示操作按钮
- 点击放大/下载

### 网格布局

- 每张图片独立操作
- 适合对比查看

### 轮播布局

- ⬅️➡️ 左右导航按钮
- 🔘 底部指示器（可点击跳转）
- 📊 图片计数器（1/3）
- 🖱️ 支持滑动
- 🎯 最大宽度 768px

## 核心实现

### 下载功能

```tsx
const handleDownload = (img: any) => {
  const link = document.createElement("a");
  link.href = img.url;
  link.download = img.alt || "image";
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
};
```

### UI 样式

- 按钮：32x32px，白色半透明背景
- 对话框：最大宽度 1280px，图片最高 75vh
- 动画：淡入淡出过渡

## 后端集成

```python
from langchain_core.messages import AIMessage

# 单张图片
AIMessage(
    content="数据分析结果",
    additional_kwargs={
        "type": "image",
        "data": {
            "url": "https://example.com/chart.png",
            "alt": "销售数据",
            "caption": "2024年销售趋势"
        }
    }
)

# 多张图片（轮播）
AIMessage(
    content="多维度分析",
    additional_kwargs={
        "type": "image",
        "data": {
            "layout": "carousel",  # 或 "grid"
            "images": [
                {"url": "...", "alt": "图1", "caption": "说明1"},
                {"url": "...", "alt": "图2", "caption": "说明2"}
            ],
            "caption": "综合分析"
        }
    }
)
```

## 轮播图优化

### 核心功能

1. **左右导航** - 40x40px 按钮，支持循环切换
2. **底部指示器** - 圆点显示位置，可点击跳转
3. **图片计数** - 显示 `1/3` 格式
4. **宽度限制** - 最大 768px，居中显示
5. **平滑滚动** - 隐藏滚动条，吸附效果

### 交互流程

```
点击按钮 → 计算索引 → 平滑滚动 → 更新指示器
```

## 注意事项

- 下载文件名使用 alt 属性
- 跨域图片可能无法下载
- 移动端悬停需点击触发

## 测试

访问 http://localhost:3000/test-components 查看示例

## 相关文件

- `custom-component-registry.tsx` - 主要实现
- `chart-preview.tsx` - 参考组件
